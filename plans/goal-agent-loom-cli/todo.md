# Goal Todos

- [ ] Research bunx/npm CLI packaging constraints and record the MVP command scope.
- [ ] Refactor the runtime entrypoint so the server can be started from a CLI command without import side effects.
- [ ] Add the `agent-loom` package executable with `start`, daemon start, `config codex`, and help/version output.
- [ ] Add targeted tests for CLI parsing and command behavior.
- [ ] Update README with Bun CLI usage, daemon behavior, and Codex configuration commands.
- [ ] Run validation and review the resulting CLI slice.
